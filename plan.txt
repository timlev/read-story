Convert (Word) document to .html in Libreoffice?LibreOffice 5.0.3.2 00m0(Build:2)
	*Anchor images as characters

"Tokenize" all words by creating a span with a unique id 1_the (lowercase and remove ! , " ? . for file_format)

Create a set of all words from ids to download to common /sounds/ folder

Try to download all words not already in /sounds/ folder
	Make a list of all homophones and decide which one to point to
	Make list of all words that couldn't be downloaded to record manually

Add <script> to html document to load, highlight, play, and unhighlight word by span

Add ".html" file link to index

SOMEDAY
Research how to preload (cache?) all audio files for a story

